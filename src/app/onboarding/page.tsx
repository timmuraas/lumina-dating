'use client';

export default function OnboardingPage() {
    return (
        <div className="min-h-screen bg-slate-950">
            {/* The OnboardingFlow is managed by AppShell based on the pathname */}
        </div>
    );
}
